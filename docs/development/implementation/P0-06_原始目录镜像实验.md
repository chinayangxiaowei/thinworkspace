# P0-06 原始目录镜像实验

## 职责与非职责

本文维护 P0-06 的准入、可执行验收断言、实验结果、审核和未完成边界；不复制物化接口、CLI 或阶段放行规则，不把实验代码称为已发布产品。

## 准入与范围

| 项目 | 值 |
|---|---|
| 类型、阶段、小阶段、风险 | Spike；P0；P0.b；R4（真实物化、FFI 与回滚） |
| 状态 | Verification；CI 卸载局部修复已审核并通过审核后本地真实验证，新候选远端 CI 待执行；PR 未合并 |
| 负责人 | 主 Agent |
| 编码模型 | GPT-5.6 Sol（`gpt-5.6-sol`）/ `xhigh` |
| 审核模型 | GPT-6 Astra（`gpt-6-astra`）/ `xhigh`；`af19a2e` 正式 Approve；CI 修复三个冻结代码文件及记录另获限定 Approve |
| 日期、任务基线 | 2026-09-13；`22c1b41332b053f36ef7d753b17910c6bd1d3ff0` |
| 来源与分支 | 已验收 main `ea9dd33ed1d4e1ab8a5b4af4921d0ef077a33517` 加已审核设计文档；`task/p0-06-raw-directory` |
| 工作区 | `/Volumes/data/code/thinworkspace-p0-06`，标准 Git linked worktree，不声明由产品创建或 CoW |
| 依赖 | P0-02 已完成；ADR-0001、ADR-0002 已接受 |
| 主要写入区 | `experiments/p0/materialize/`，复用并收缩旧物化实验，不创建平行物化器 |
| 配套变更 | 本记录、实施计划、文档索引；必要质量配置由主 Agent 协调 |

权威输入：[物化设计](../../project/design/ThinWorkspace_跨平台工作区物化设计_v1.0.md) §3、§5–7、§9，[技术栈](../../project/reference/技术栈.md) §4–6、§11–13，[开发规范](../process/开发规范.md) §5–7、§14–16，[任务流程](../process/任务流程.md)及[实施计划](../planning/ThinWorkspace_Phase1实施计划_v1.0.md) §3。涉及行为歧义先通知主 Agent，不自行扩展保证。

本任务只验证原始目录物化及保真范围，不实现 P1 CLI、GitInspector、普通/强制删除服务、日志设施、SQLite、缓存策略、Git 重定位或活跃源的原子快照。P0-07、P0-05、P0-04 按计划继续。

### 实施顺序与旧路线处置

1. 先以失败测试证明源 `.git` 被拒绝和旧目标控制项前提；移除该前提，统一使用空目标、全树 manifest 和登记对象回滚。旧“保护 `.git`”断言改为普通非空目标拒绝/树外 sentinel 保护；不保留旧兼容开关。
2. 再以失败测试补普通文件、嵌套目录及源根目录 mtime；两后端显式满足同一范围，源变化不能误报成功。
3. 补其余原始目录/失败边界并执行专项门禁，审核后真实 release 验证；全任务退出条件未满足前不标 Done。

旧 P0-03 未合并代码和 18 项未提交输入留在原工作区与分支，新基线没有导入它们；main 的 Cargo/CI/fuzz 配置原样保留，不以删除测试消除失败。P0-02 的 Git sentinel 前提已被 ADR-0002 替代，但其身份校验、未知项停止、CoW 与显式降级安全断言继续保留并回归。历史实验记录不改写为本任务证据。

## 验收断言

| 场景 | 可检查的结果 |
|---|---|
| 非 Git 源与完整内容 | 两后端都物化嵌套普通文件、`.git` 目录/普通指针文件、ignored/未跟踪命名、构建产物；按磁盘内容复制，不调用 Git、不筛选名称 |
| 非空目标 | 包括预建 `.git` 在内的任意既有条目均拒绝，既有内容不变；无隐藏控制项豁免 |
| mtime/权限 | 普通文件、嵌套目录、空目录与源根目录按源实际时间精度比较；目录在子项完成后恢复权限和 mtime；不宣称其他元数据或缓存命中保证 |
| 链接与硬链接 | symlink 文本原样复制，不访问树外目标；普通硬链接目录项成为独立副本，目标写入不改变源及其他目标副本 |
| 空树与无普通文件 | 创建成功、CoW not-used；包含普通文件且全部真实克隆成功并校验后才 confirmed |
| 源变化与布局 | 源/目标重叠拒绝；在已验证观察边界修改源内容、结构或受保证元数据时拒绝成功；证明可发现的变化，不称为原子快照 |
| 特殊项/子挂载 | 明确失败且不静默跳过，不克隆或清理挂载外数据；真实环境未准备时记录未执行 |
| 回滚与未知对象 | `.git` 与其他已登记条目同样回滚；完整身份集不符、登记失败或回滚失败时保留现场；树外 sentinel/指针目标不变 |
| 回归 | 同卷真实 clone、跨卷 EXDEV、显式 Full Copy、不允许降级的 errno、未知项停止与重复扫描等 P0-02 安全覆盖仍通过 |

## 运行与清理边界

普通用例由既有受控 fixture 工厂在本工作区 `target/` 下创建私有数据；子挂载用例为满足源/目标同卷前提，在 `/private/tmp` 下创建工厂登记的私有目标。均不使用用户源码或真实缓存。外部 sentinel 也位于本次工厂登记范围内。失败现场按身份证据保留，不用 `remove_dir_all`、shell/glob 或默认 TempDir 析构绕过停止结论。跨卷/子挂载环境由主 Agent 新建专用临时镜像后记录实际路径和 UUID；测试只读借用子挂载源，不管理卷。测试进程和挂载未经确认终态不重启、不强制清理。

## 预定验证命令

```bash
cargo test -p thinws-p0-materialize --test raw_directory
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace --all-targets
cargo test --release -p thinws-p0-materialize --all-targets -- --include-ignored --nocapture
cargo mutants -p thinws-p0-materialize --leak-dirs --timeout 180 --jobs 2 --output target/p0-06-mutants -- -- --include-ignored --nocapture
cargo +nightly-2026-08-14 fuzz run thinws_materialize_layout_policy -- -max_total_time=60 -timeout=5 -max_len=4096 -print_final_stats=1
```

包含 ignored 测试的命令须先设置并核验专用 `THINWS_P0_CROSS_VOLUME_ROOT` 与 `THINWS_P0_SUBMOUNT_SOURCE`，本次实际路径和身份见下方证据。变异和 fuzz 使用 `tools/quality-tools.toml` 固定工具；若代码位置变化导致精确等价排除不再适用，由主 Agent 和规定 Reviewer 重新核对，不沿用过期行号或排除完整函数。源码改动期间不并发跑变异。

## 当前证据

- 编码前基线：macOS 15.7.2 / arm64；fmt、Clippy、普通 workspace 测试终态退出 0，109 passed、2 ignored。两项需额外 APFS 卷的测试在准入时未执行，不算本任务跨卷验收。
- 发布基线的 15 份 Markdown 与 `4562065` 对应 blob 逐一一致；GPT-6 Astra / `xhigh` 已审核其集成边界并 Approve。该结论只覆盖文档，代码另按下方子步审核和验证。
- GitHub PR #1–#3 经实时查询均已合并且检查通过；新设计文档单独提交并推送，后续代码不得混入该文档候选的验证声明。
- 新设计文档发布为 [PR #4](https://github.com/chinayangxiaowei/thinworkspace/pull/4)，head 为上述任务基线。首次创建请求返回 HTTP 502，但主 Agent 随后查询确认 PR 已创建，未重复创建。[CI 34748409509](https://github.com/chinayangxiaowei/thinworkspace/actions/runs/34748409509) 已终态成功：真实双卷 debug/release 各 111 passed、0 ignored；全实验变异 514 项，418 caught、96 unviable；路径与布局 fuzz 分别 722,854 / 2,722,746 次、无 crash/hang，依赖检查和普通卸载成功。这些是文档候选的旧实验基线，不替代下方新代码证据。
- 合并前重新获取 main，仍为 `ea9dd33ed1d4e1ab8a5b4af4921d0ef077a33517`，核对已审核 head 和绿色 CI 后，以精确 head 约束合并 PR #4。GitHub 确认 merge commit 为 `3353dedae45eaf23c34969ad0361687cbbebfa42`（2026-09-13）；该 merge 的树与文档 head 完全相同。源码子步不混入此 PR，继续按独立代码任务交付。
- 已在新建私有父目录 `/private/tmp/thinws-p006-apfs.ODdevL` 创建专用 128 MiB 镜像 `cross-volume.dmg`，普通挂载到其 `mounted`，开启 ownership。`diskutil` 观察挂载 Volume UUID 为 `C25D1051-142F-423E-BCD2-1E07DAA4246E`，工作区所在 `/Volumes/data` 为 `1A42C888-32E3-489C-9BFA-67FD640A94E8`；既有真实跨卷 Probe 用例独立验证通过（1 passed）。镜像与挂载由主 Agent 持有，当前仍用于验证；不卸载或修改其他应用的镜像。
- 原始内容子步的 RED/GREEN、限定审核和 release Verification 见下方；其余验收断言、全任务专项门禁及代码 PR 合并待完成。当前不作实验 Go 或 P0/P1 放行。

### 原始内容首项 RED/GREEN

GPT-5.6 Sol / `xhigh` 先新增 `raw_directory` 集成测试。首次测试自身的编译错误修复后，执行下列命令：

```bash
cargo test -p thinws-p0-materialize --test raw_directory full_copy_materializes_source_git_directory_as_ordinary_content -- --exact --nocapture
```

有效 RED：编译成功，退出 101，0 passed / 1 failed；失败来自旧实现 `source contains the reserved .git control entry`，不是环境或编译问题。移除源/目标 Git 特判后，同一命令退出 0，1 passed / 0 failed / 0 ignored。此处只记录该精确断言，不把它当作其余目录、mtime 或回滚保证的通过证据；本子步其余门禁见下节。

### 原始内容子步验证（不代表整个 P0-06 完成）

该子步中，Sol 完成四个实验文件的限定修改并停止写入：移除 Git 特判、严格空目标、全树 manifest/登记回滚、空集合 CoW 语义及 7 个新测试。没有引入新的物化器、兼容开关或产品对象；该次冻结不包含 mtime/新元数据 FFI 与子挂载实现，后续进度见下节。

- 主 Agent 在冻结源码上独立执行 fmt、workspace Clippy、普通 workspace 测试：全部退出 0，116 passed / 2 ignored；随后使用上述专用卷运行 `THINWS_P0_CROSS_VOLUME_ROOT=/private/tmp/thinws-p006-apfs.ODdevL/mounted cargo test --release --workspace --all-targets -- --include-ignored --nocapture`，终态退出 0，118 passed / 0 failed / 0 ignored。FD-zero 子进程输出不重复计数。
- 既有布局 fuzz 使用固定 nightly/工具，按上方命令执行 60 秒预算，终态退出 0；实际 61 秒、3,936,035 次、无 crash/hang。该纯内存 harness 验证既有路径组合策略，不冒称覆盖尚未实现的 mtime 或真实文件删除。自动生成 corpus 留在本工作区，未作为回归样本全部提交。
- 增量变异覆盖本次受影响的 10 个函数：`prepare_attempt`、`execute_prepared_inner`、`snapshot_tree`、`snapshot_directory`、`verify_target_empty`、`observe_target_entries`、`validate_rollback_set`、`prepare_fresh_copy_after_clone`、`rollback_created`、`apply_rollback_fault`。完整枚举为 39 项；首批正则匹配了 27 个运算/分支项，发现未包括函数返回值命名后，核对并补跑另外 12 项。两批互不重叠且并集等于完整枚举；汇总 30 caught、8 unviable、1 missed、0 timeout。原始结果保留在 `target/p006a-mutants/` 和 `target/p006a-mutants-return/`，不删除首批失败记录。
- 上述唯一 missed 为 `lib.rs:795:35` 将 `context.clone_calls_succeeded == regular_count` 的 match guard 改为 `true`。GPT-6 Astra / `xhigh` 已逐项核验为当前实现下的精确等价：计数由零开始，仅在真实 clone 成功后唯一递增；任何克隆、登记或后验失败都退出成功路径；最终全树 manifest 与登记身份集合一致，保证普通文件项数等于该计数，零项已由前一 guard 分流。因此保留防御性原判断，只在 `.cargo/mutants.toml` 精确排除这一位置和替换，不排除函数或文件。负责人为主 Agent；引入单文件复制/跳过/去重、重试计数复用、吞错继续、改变计数规则或放宽最终集合校验时，必须撤销并重审；行号/表达式变化也须重新核对。
- 规定模型 GPT-6 Astra（`gpt-6-astra`）/ `xhigh` 已对四文件和准入文档给出限定 Approve，接受上述等价性；未运行测试，其结论不覆盖本任务其余验收项或阶段放行。主 Agent 在源码未变化的前提下执行审核后 release Verification，仍为 118 passed / 0 failed / 0 ignored、终态退出 0。
- 精确排除后，使用下列统一函数正则重跑增量变异，实际执行 38 项：30 caught、8 unviable、0 missed、0 timeout，终态退出 0。结果和运行版本见 `target/p006a-mutants-verified/mutants.out/outcomes.json`（`cargo-mutants 27.1.0`）；这是当前子步增量门禁，不是上方预定的全任务全 crate 门禁。
- GPT-6 Astra / `xhigh` 补充核对了最终排除配置、终态 JSON、四文件冻结哈希和证据范围，维持限定 Approve；确认没有扩大排除或把本任务标为 Done。该补充核对未重跑测试。

```bash
THINWS_P0_CROSS_VOLUME_ROOT=/private/tmp/thinws-p006-apfs.ODdevL/mounted cargo mutants -p thinws-p0-materialize --leak-dirs --timeout 180 --jobs 2 --output target/p006a-mutants-verified --re '(prepare_attempt|execute_prepared_inner|snapshot_tree|snapshot_directory|verify_target_empty|observe_target_entries|validate_rollback_set|prepare_fresh_copy_after_clone|rollback_created|apply_rollback_fault)' -- -- --include-ignored --nocapture
```

安全失败用例仍按既有规则保留无法确认身份的现场，变异 scratch 使用 `--leak-dirs` 保留；不以默认析构或 shell 清理绕过停止。剩余工作继续按本任务实施顺序完成，不从本子步通过推导 P0-06 Done。

该子步已本地提交为 `174891ee7634d3b7a64cd74f1aea674a6c38fa66`（`Task: P0-06`）；尚未推送，后续子步沿用此源码基线。代码 PR 在 P0-06 验收范围补齐后统一提交，避免把部分通过称为全任务完成。

### 其余保真与边界验证（进行中）

- 两路编码均为 GPT-5.6 Sol / `xhigh`，写入区分离：mtime 维护物化源及相关测试；身份隔离/子挂载只新增独立测试文件，不改生产实现。规定审核为 GPT-6 Astra / `xhigh`。
- `identity_isolation.rs` 的两项 characterization 测试各覆盖两后端：真实源硬链接成为彼此及与源均不同的文件身份，双向修改字节互不影响；按登记身份删除测试源文件和目录后，目标仍可读写。编码 Agent 定向 debug/release 各 2 passed，单文件格式、Clippy、diff 检查通过；Astra 对 SHA-256 `1fcd1daf296da8cb488c128779f3c0dad89df4e842471074bf029d393d1eed97` 给出限定 Approve，未运行测试。首次即符合既有预期，不伪造 RED；最终组合需由主 Agent 冻结后复验。
- mtime 的有效 RED 分别独立执行 `cargo test -p thinws-p0-materialize --test raw_directory apfs_clone_preserves_root_directory_and_regular_file_mtime_and_permissions -- --exact --nocapture` 及对应的 `full_copy_preserves_root_directory_and_regular_file_mtime_and_permissions`：两次均编译成功后退出 101。Clone 的根权限仍为 0700，根/嵌套/空目录 mtime 未保留；Full Copy 还缺普通文件 mtime。最早的循环测试只到 Clone 失败，不计作两后端证据；首次 fixture setup 在只读父目录下建文件失败不算有效 RED。
- 初次 mtime GREEN 已覆盖上述两后端各 1 项、真实 futimens 的 atime omission/mtime/非法纳秒、准备后 root/dir/file mtime 变化拒绝、只读子目录使失败回滚安全停止、空树根属性阶段失败的 Partial/Incomplete。Sol 的该次定向物化测试为 75 passed / 1 ignored，专用跨卷另跑 1 passed，crate 格式与 Clippy 通过；这不等于最终组合门禁。
- Astra 对初次冻结 mtime 给出 **Changes requested**：克隆目标按名重开后，属性写入前缺少 held FD 身份核验；根收尾的可写性例外未区分 Unknown、mode 未变或其他不可归因变化。主 Agent 独立确认第一项，接受两项阻断，已要求先补回归再做局部修复。禁止通过自动给目录增加写权限、另建权限救援/ACL复制机制解决；失败允许保留现场，不谎报恢复基线。未修复并重审前不批准该子步。
- 初次 mtime 冻结使原 CoW guard 等价排除从 795 行移至 858 行；Astra 确认语义证明仍成立，但旧配置尚未更新。后续修复和子挂载会改变最终行号，由主 Agent 最终枚举并精确同步后再执行变异门禁，不沿用过期位置。
- 初次 setup 失败现场 `/Volumes/data/code/thinworkspace-p0-06/target/p0-materialize-tests/fixture-parent-metadata-clone-82811-0` 已丢失进程内活登记，按规则保留，未做无证据删除。

子挂载环境与 RED：主 Agent 新建 `/private/tmp/thinws-p006-submount.W2HgP1/submount.dmg`，挂载于其 `source/mounted`；借用源仅为 `source`，镜像文件位于源外。源所在 System/Data Volume UUID 为 `382DF1EF-8A38-4E30-8445-613B5192C6DE`（device 16777229），子卷为 `B961DCA9-818A-4784-AB67-92CA2DE24750`（device 16777256），ownership 开启。另一次在工作区 `target/p006-submount.4GhKlb/` 创建的镜像挂载返回权限错误，`hdiutil info` 已确认未挂载；该镜像、目录及自动生成标记原样保留，不改宿主权限以绕过错误。

`submount.rs` 的两个 ignored 用例分别使用 `THINWS_P0_SUBMOUNT_SOURCE=/private/tmp/thinws-p006-submount.W2HgP1/source cargo test -p thinws-p0-materialize --test submount <backend>_prepare_rejects_real_source_submount_before_writing -- --exact --ignored --nocapture`（`<backend>` 分别为 `apfs_clone`、`full_copy`）。两次编译成功、各 0 passed / 1 failed、退出 101，准确失败于 `prepare_attempt` 返回 Ok；源/目标同设备、子挂载不同设备，未以普通跨卷预检失败冒充子挂载拒绝。测试只读借用源并核对身份/唯一子项不变，受控目标始终为空且精确清理。Sol 随后在源快照、执行遍历和回滚目标观察中加入相对根 device 的子挂载拒绝，未加挂载管理器；上述两项各自 GREEN（1 passed）。CI 配套把既有额外卷放在独立源目录的 `mounted` 下，使跨卷与子挂载测试复用同一镜像；尚未在新代码候选执行，不宣称 CI 已验证此改动。

### 安全修复与组合门禁（进行中）

Sol 对两项阻断分别补了有效 RED：`metadata_identity_mismatch_does_not_change_mode_or_mtime` 在旧实现修改了错误 held FD 对象属性而返回成功；`final_target_writability_exception_rejects_unknown_and_unexplained_mode_changes` 在旧实现放行了 mode 不变的写权限变化，均编译成功后退出 101。局部修复把 held FD 身份/类型验证放在属性修改及计数副作用之前，并将根收尾例外限制为可归因的已知权限变化。主 Agent 追加 `0770→0750` 反例（只去除 group-write，owner-write 仍在），同一单测再次有效 RED；最终要求 owner-write 位确实由有到无，且 Allowed→Denied(EACCES)、证据来源一致、其余权限事实不变，拒绝 Unknown。不创建 uid/group/ACL 推断框架或失败后的权限救援。

上述两项单测 GREEN；两个真实 0555 根目录的后端用例各 1 passed，crate fmt/Clippy 通过。Sol 冻结后，主 Agent 仅更正一行过期实验 rustdoc；该次 `lib.rs` SHA-256 为 `d9a27d6b93d8c8173fd93b882ba54bfdd254f500341cd22e08549b1d7870837b`，`ffi.rs` 为 `41a764d0198de547c7ff0b80e052353f09267b71eb71d3f82df12aa2b111fcb1`。其他测试哈希由 Agent 交付后由主 Agent 独立核对；以下首轮组合门禁期间源码未变化。GPT-6 Astra / `xhigh` 已独立复核安全修复、子挂载、CI 和原等价项，给出限定 Approve，未运行测试；明确不覆盖下方尚未通过的变异门禁或整个任务完成。

- 主 Agent 执行该次组合 fmt、workspace Clippy：退出 0。普通 workspace 测试退出 0，126 passed / 4 ignored；4 项分别需要跨卷或子挂载环境，不算通过。
- 独立重查两个专用 APFS UUID 未变化后，设置上述两个环境变量执行 `cargo test --release --workspace --all-targets -- --include-ignored --nocapture`，退出 0，130 passed / 0 failed / 0 ignored。子进程的 FD-zero 单项不重复计数。这是复审前组合自测，不能替代审核后 Verification。
- 同一组合的 debug `cargo test --workspace --all-targets -- --include-ignored --nocapture` 随后也终态退出 0，130 passed / 0 failed / 0 ignored。fuzz workspace fmt/Clippy 通过。两个 workspace 的 `cargo deny --locked ... check` 与 `cargo audit --deny warnings --file ...` 均退出 0；deny 仅有共用配置中另一 workspace 的未使用 license exception/allowance 提示，未修改许可政策。实际 mutants/fuzz/deny/audit 工具版本与 `tools/quality-tools.toml` 一致。
- 主 Agent 用 `cargo mutants -p thinws-p0-materialize --list --json --no-config` 实际枚举，确认同一 CoW guard 现为 `859:35`，既有 FFI 旗标等价项分别从 27/39/49/59/69 行移至 29/41/51/61/71 行，表达式和列未变；该次只移动既有精确排除，没有增加或扩大排除。配置后枚举为 314 项，相关旧等价项均已精确匹配，语义证明随后由规定 Reviewer 复核接受。
- 首轮全任务变异：上方两个环境变量加 `cargo mutants -p thinws-p0-materialize --leak-dirs --timeout 180 --jobs 2 --output target/p006-final-mutants -- -- --include-ignored --nocapture`。原版 baseline 通过；全 314 项终态退出 2，耗时约 8 分钟，252 caught、46 unviable、16 missed、0 timeout。原始输出保留在 `target/p006-final-mutants/mutants.out/outcomes.json`；未删除失败结果，不能报告该门禁通过。
- 上述 16 个存活中，15 个是最终重验、权限例外、根回滚和元数据比较的负例覆盖缺口，已授权 Sol 只在既有私有单测模块补最小表驱动测试；不改生产逻辑、不新增故障钩子或框架，也不扩大排除。整函数 `revalidate_held_roots_after_target_metadata → Ok(())` 不能视作等价。
- 唯一精确等价为 `lib.rs:1000:9` 将 `is_expected_target_writability_change` 最后一个 `||` 改为 `&&`。Astra / `xhigh` 独立核验真实 diff：`ExecutionGuarantee` 与 `WriteAttempt` 当前各为单一无 payload variant、派生相等比较，其两个不等式恒为 false；按 Rust 优先级，原式 `a || b || false || false` 与变异式 `a || b || (false && false)` 均为 `a || b`。主 Agent 仅将该位置/函数/替换加入精确排除。负责人为主 Agent；任一枚举新增 variant/payload、字段或相等语义改变、表达式分组/操作数改变或行号迁移时必须撤销并重审。相邻 `999:9` 会掩盖 mount 差异，不在排除范围，必须补测试。
- 布局 fuzz 使用预定 60 秒预算，终态退出 0；实际 61 秒、2,370,294 次输入、无 crash/hang。它只覆盖既有纯内存布局策略，不冒称测试了真实元数据系统调用或目录删除；自动新增 corpus 仍保留在工作区，未批量提交。

### 补测与本地复验

Sol 新增 7 个单元测试并扩充 1 个既有测试，覆盖四个根目录的最终重验、错误 held FD、目标 mode/mtime、非目标权限例外、根回滚和元数据字段独立差异。`before_denied` 用例经主 Agent 校正为仅 before 状态不同，证据来源及 after 保持一致，避免多个条件同时变化掩盖缺口。

写入仅限 `lib.rs` 的 `#[cfg(test)]` 模块，没有增加生产钩子、权限框架或新排除。最终文件 SHA-256 为 `ff76e2aa07fc1232fe899be92712b771addcc7a99ec395f4d77904f40fd5a830`；测试模块前的生产前缀 SHA-256 仍为 `86e7d8974d33848fbe3a1a715146d644f88fff4974751d24e058378adfde0908`，与首轮变异期间一致。包含唯一新增等价项的配置 SHA-256 为 `a252e10e4b591e26c3215cf7593390509615fb3b93d635dbd577349386a9c80c`。

- 主 Agent 在重新冻结后执行 fmt、workspace Clippy，均退出 0；普通 workspace 测试为 133 passed / 0 failed / 4 ignored。
- GPT-6 Astra / `xhigh` 独立核验测试差异、15 项非等价存活的对应断言、受控权限恢复/清理和三个冻结哈希，给出限定 Approve；未运行测试，不以此代替变异终态。
- 随后主 Agent 使用上述真实跨卷/子挂载环境执行审核后 release Verification：`cargo test --release --workspace --all-targets -- --include-ignored --nocapture`，终态退出 0，137 passed / 0 failed / 0 ignored，FD-zero 子进程不重复计数。
- 重新枚举为 313 项，只有已审核的那一个等价项从首轮集合排除；全量重跑沿用原命令和两个环境变量，输出目录改为 `target/p006-final-mutants-verified`。终态退出 0，约 7 分钟，267 caught、46 unviable、0 missed、0 timeout。主 Agent 将首轮名单与终态结果逐项连接，确认 15 个非等价存活全部转为 caught；首轮失败结果及两轮 scratch 均保留。

远端 main 经重新获取仍为 `3353dedae45eaf23c34969ad0361687cbbebfa42`，其树与本任务文档基线 `22c1b41` 相同；当前无其他开放 PR。旧工作区的 18 项未提交输入再次逐一比对均未改变。

本地验收没有新增依赖或改变产品 CLI。两后端的原始内容、首版权限/mtime、硬链接隔离、源删除存续、真实子挂载拒绝和既有回滚/跨卷断言均已执行；完整代码候选按上述测试结果提交供审核。P0-07、P0-05、P0-04 仍未实施，不能从本任务结果推导这些能力完成。

该检查点进入 In Review，代码 PR/远端 CI 与合并仍待完成，未标 Done 或阶段放行。当时两个专用 APFS 卷继续保留用于候选复验；后续卸载与远端结果见下节。历史失败现场、scratch、镜像和当前工作区保留为实验依据，不执行无归属证据的删除。

### 代码候选发布与 CI 卸载失败

完整代码候选 `af19a2ec5da34c96f4ec2e46fb9fa3bdf0908a50`（包含原始内容子步 `174891e`）已推送至 [PR #5](https://github.com/chinayangxiaowei/thinworkspace/pull/5)。GPT-6 Astra / `xhigh` 独立核对提交文件、冻结哈希、最终变异 JSON 和证据范围，正式 Approve；审核未代跑测试，也未授权跳过 CI。主 Agent 重新获取 main，仍为 `3353dedae45eaf23c34969ad0361687cbbebfa42`；旧 P0-03 的 18 项未提交输入再次逐项比对无变化。

本地测试及变异进程终态后，主 Agent 核对上文两个精确镜像关联与 Volume UUID，分别普通卸载 `cross-volume.dmg` 和 `submount.dmg` 的挂载点，均退出 0；随后 `hdiutil info` 中已无这两个镜像的关联。镜像文件、失败现场、scratch 和 corpus 保留，未删除、未强制卸载，也未操作其他镜像。上文环境变量此后不能直接用于测试，重用时必须重新挂载并验证身份。

[CI 34751606196](https://github.com/chinayangxiaowei/thinworkspace/actions/runs/34751606196) 针对上述精确候选于 2026-09-13 终态 **failure**。逐项真实日志证明：

- 格式、Clippy、依赖检查通过；真实 APFS debug/release 各 137 passed、0 failed、0 ignored，FD-zero 子进程不重复计数。
- 全 workspace 557 项变异在约 23 分钟内完成：460 caught、97 unviable、0 missed、0 timeout；步骤成功。
- 路径与布局 fuzz 分别执行 819,226 / 4,126,588 次，均无 crash/hang、步骤成功。
- 唯一失败是最后的 `Detach experiment volume`：先对挂载路径普通卸载，返回 16 / Resource busy；1 秒后仍以原挂载路径重试，返回 1 / No such file or directory。日志不足以证明整个镜像已卸载，不能将此 CI 写成通过。

任务按 Verification 失败规则返回 In Progress。局部修复只处理 CI 自有镜像的卸载与最小回归测试：从新鲜 `hdiutil info -plist` 确认精确镜像关联，避免部分卸载后继续依赖消失的挂载路径；不使用 force、不忽略未知状态、不降低测试门槛、不改物化 Rust 或产品设计。编码仍为 Sol / `xhigh`，后续由 Astra / `xhigh` 审核；本节不是修复已完成或新 CI 已通过的声明。

主 Agent 为真实卸载验证新建唯一私有根 `/private/tmp/thinws-p006-ci-detach.8YOad1`，其中 `thinws-p0-cross-volume.dmg` 为 128 MiB APFS 镜像，挂载点为 `thinws-p0-submount-source/mounted`，Volume UUID 为 `9152D91C-FE3F-4861-9E2D-E8882C33B4DE`。准备时的 `hdiutil info -plist` 确认精确 image-path 下同时含 GUID 物理盘及 APFS 合成设备；不能把“只有一个 whole disk”当成正常 APFS 的前提。后续只验证这一个自有镜像的普通卸载与“卷已 unmount、镜像仍附着”状态，不触碰其他镜像或删除文件；此检查点仅完成环境准备。

主 Agent 的真实 RED：对该自有挂载点执行 `hdiutil unmount`（退出 0），随即从 `hdiutil info -plist` 确认同一 image-path 和设备关联仍在，但已无 mount-point 字段。再从 `git show af19a2e:.github/workflows/p0.yml` 提取旧 `Detach experiment volume` 的原始 run 内容，用该 fixture 的实际环境变量和 `/bin/bash -e -c` 执行，退出 1，准确失败于原挂载路径不存在。此测试直接覆盖“镜像仍附着、挂载路径已消失”的真实状态；不声称复现了远端第一次 Resource busy 的具体占用者。镜像保持附着以供新脚本对同一现场验证，尚未执行修复后的 GREEN。

### CI 卸载局部修复与自测

Sol / `xhigh` 将原 CI 卸载步骤替换为标准库脚本 `tools/ci_detach_image.py`，对应单测进入同一 workflow 的 `Test CI helpers` 步骤。脚本只接受 runner 临时根下固定镜像名，每次从新鲜 plist 找到该精确 image-path 下唯一 GUID partition scheme 设备，再调用固定 `/usr/bin/hdiutil` 普通卸载；成功后仍须确认镜像关联消失。保留最多 5 次 busy 重试和 1/2/4/8 秒间隔，查询/卸载分别有 10/30 秒 timeout；查询失败、格式或关联不明、非 busy 错误均失败，不解析 stderr 决定状态、不使用 force。此脚本仅适用于本 CI 自建的默认 GUID 镜像，不是产品 Adapter 或通用卷管理器，不新增 Python 依赖或改变 Rust 门槛。

- 自动化 RED：`python3 -m unittest tools.test_ci_detach_image.DetachImageTests.test_busy_retry_re_resolves_image_to_device_after_mountpoint_disappears -v` 在旧算法上退出 1，准确失败于 busy 后继续对原挂载路径调用而获得 exit 1，不是 import 或编译失败。
- GREEN：主 Agent 独立执行 `python3 -B -m unittest tools/test_ci_detach_image.py -v`，11 项通过。包含 busy 后无挂载路径、正常卸载、已无镜像、非 busy、查询/解析错误、路径/关联歧义、重试耗尽和成功后复查；malformed XML 的实际 `ExpatError` 也明确处理。
- 真实同现场 GREEN：对上一节仍附着且无 mount-point 的自有镜像执行 `python3 -B tools/ci_detach_image.py --image /private/tmp/thinws-p006-ci-detach.8YOad1/thinws-p0-cross-volume.dmg --runner-temp /private/tmp/thinws-p006-ci-detach.8YOad1`，退出 0；独立 `hdiutil info -plist` 查询已无该镜像关联。
- 真实正常场景：重新挂载同一镜像并独立核对 UUID 不变，执行同一脚本退出 0，随后无关联；其他镜像的路径/设备关联指纹前后相同。已无关联时再执行脚本也退出 0，无额外卸载。镜像和临时根保留，没有删除文件。
- 通用 fmt、workspace Clippy、普通测试再次退出 0，133 passed / 4 ignored；Rust 文件、Cargo/lockfile 和变异排除均未修改。此次局部修复没有重跑 Rust 全量变异或 fuzz；上节是原代码候选的远端证据，新候选的完整 CI 仍须执行。新辅助脚本用标准库单测及真实镜像验证，不宣称受 `cargo-mutants` 或 Rust fuzz 覆盖。

本检查点三个代码文件冻结 SHA-256：workflow `ef5de0795cfaa9ff4a16c6c21bd3df2f33c9f863df1f500383fba7c8c639e490`，脚本 `955ae7f37174190222c2f0b0f620a7e4618e5f622d24cb5b493b76d9ec2d6a1c`，单测 `01b464484a50c20947ca87654874cbad9114c6023fc3ada7620c8a7cf17f2dba`。规定模型审核、审核后 Verification、新提交与远端 CI 待完成；不提前合并或标 Done。生成的 `tools/__pycache__/` 仅本地保留，不纳入提交。

随后 GPT-6 Astra / `xhigh` 核验这三个冻结文件及两份文档差异，独立运行全部 11 项模拟单测，给出限定 Approve，未操作真实镜像。主 Agent 在同一代码哈希上执行审核后 Verification：11 项单测再次通过；同一个已登记镜像分别验证正常挂载、先 unmount 但镜像仍附着、已无关联三个场景，脚本均退出 0。每次挂载都独立核对上述 UUID，最终独立 info 查询无该镜像关联，其他镜像路径/设备关联指纹保持不变；镜像文件和历史失败材料仍保留，无 force 或文件删除。任务进入 Verification，修复提交后的完整远端 CI 与 PR 合并仍待完成；旧 CI failure 不被覆盖，也不将阶段标为放行。
