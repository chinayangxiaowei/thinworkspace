# P0-05 文件副本锁隔离实验

## 职责与非职责

本文维护 P0-05 的实施准入、实际命令、实验结果、审核证据和未完成边界。完整验证矩阵只引用[文件克隆与锁语义调研 §7](../../project/reference/ThinWorkspace_文件克隆与锁语义调研.md)，不复制矩阵或重新定义物化、缓存、ProcessProbe 和 CLI 契约。

本实验只回答独立文件副本是否隔离已知锁及相关风险，不预先批准“扫描后重写”功能，不代表 P0/P1 阶段放行。

## 准入与领取

| 项目 | 当前事实 |
|---|---|
| 类型、阶段、小阶段、风险 | Spike；P0；P0.b；R4（真实文件系统、双进程锁、受控替换与时间属性实验） |
| 任务状态 | 以[实施计划](../planning/ThinWorkspace_Phase1实施计划_v1.0.md)为准；In Review，首轮 L1–L7 和冻结候选本地门禁已完成，待最终精确审核、审核后 Verification、PR CI 与合并 |
| 负责人、模型 | 主 Agent；编码指定 GPT-5.6 Sol / `xhigh`，审核指定 GPT-6 Astra / `xhigh`；实际执行与审核结果据后文补记 |
| 日期、实际基线 | 2026-09-13；`d410e259e1b74a297dd27f612e80f9acbfec50a6`，已从 origin 读取的 PR #7 合并结果，树与已审核/CI 通过的 `3a06e22` 相同 |
| 分支、工作区 | `task/p0-05-lock-isolation`；`/Volumes/data/code/thinworkspace-p0-05`；标准 Git linked worktree，不是产品 CoW 工作区 |
| 已有修改 | 新 worktree 建立时无本任务已有修改；不携入 P0-07 的未跟踪 fuzz corpus 或历史实验现场 |
| 本机环境、准入根 | macOS 15.7.2 / arm64；`target/p0-locks-admission.eQWI1O`，mode 0700、device 16777240、inode 35539222；APFS Volume UUID `1A42C888-32E3-489C-9BFA-67FD640A94E8` |
| 主要写入区 | 独立的 `experiments/p0/locks/` 集成实验，作为既有 materialize crate 的独立 test target 接线；本文、计划和索引。无新产品模块、crate 或锁 FFI |

技术依赖 P0-02 已完成，P0-07 已按实施计划 §3.1 的顺序合并；本工作区从准备基线 `3a06e22` 安全快进到上述 main，保留自己的两份文档草稿，没有携入旧现场。随后基线 fmt 和普通 workspace 全 targets 实际退出 0：246 passed、4 既有 ignored；新工作区不会自动继承 CI 的额外卷环境。

准入根由 `mktemp -d` 在本工作区 `target/` 下新建；使用当前基线的 `thinws-p0-probe path` 与 `stat` 核对上述身份、权限及可写预检，均 exit 0。Probe 没有执行 clone，不能据此记录锁隔离或 CoW confirmed。自动测试每次另建并记录自己名下的私有 case，不依赖这一本机随机路径；不允许访问或清理此前任务的夹具。

权威输入：上述调研 §3–8、实施计划 §3.2、[跨平台物化设计](../../project/design/ThinWorkspace_跨平台工作区物化设计_v1.0.md) §3–7/§9、[技术栈](../../project/reference/技术栈.md)的 macOS/测试选型，以及[开发规范](../process/开发规范.md)的文件系统、内部子进程和测试章节。门禁范围与等待方式遵循[任务流程](../process/任务流程.md) §18，不在本实验复制等待规则。

## 已核对的复用点（不是实测结果）

- 本机锁定的 `rustix 1.1.4` 提供 `fs::flock` 和 `fs::fcntl_lock` 安全 API，macOS 未被其条件编译排除。其 libc 实现把非阻塞独占的 `fcntl_lock` 映射为 `F_SETLK/F_WRLCK`，范围从偏移 0 到文件末尾（`l_len=0`），符合首轮重叠范围验证。可先复用已有依赖，不能因需要两种锁而另引 `nix` 或复制一套 FFI。
- 已有 `thinws-p0-materialize::materialize_once` 接收 `MaterializeRequest` 和显式 `Backend::ApfsFileClone/FullCopy`，返回实际调用及文件身份等证据。准备采用该入口验证独立副本，不用 `cp -c` 或复制成功的布尔值替代 CoW Receipt。
- 已有 materialize 测试的 `ControlledTree` 没有 Drop 清理，并保存 no-follow 身份记录；后续按其现有能力评估复用，不为本实验改写其他任务的清理边界，也不把历史验证自动计入新实验。

## 已冻结的执行约束

1. 持锁者与竞争者必须是独立进程，分别打开目标；子进程只运行当前测试二进制的固定内部入口。固定收到加锁成功的握手后才执行物化或竞争，不用 sleep 猜测锁状态。握手/消息读写上限 5 秒，单子进程总观察上限 30 秒，诊断输出上限 64 KiB；异常只终止自有 Child，另给最多 1 秒确认退出，无法确认则本场景失败并保留现场，不无限 wait。
2. 持锁者在观察期间保持同一个 FD 和锁，不在该进程读取摘要或额外打开/关闭该文件，避免传统 POSIX 锁因关闭同文件其他 FD 而释放。协调进程负责物化、内容/身份记录和对照。
3. 每种锁分别建立同文件/别名冲突阳性对照；对照无效立即停止该场景，不能继续报告副本隔离。任何失败或超时保留原结果，不通过改写断言迁就文献预期。
4. 所有写入、硬链接/符号链接、rename、时间恢复及权限拒绝场景只使用新建实验树和自有进程。替换不迁移旧 FD；原地写入不预设新 inode；不扫描或修改真实用户缓存，不删除 `.lock` 标记，不终止外部进程。
5. 明确普通结束、中断和失败时的处置：优先保留文件与证据；终止只针对本次直接创建且身份可核对的测试子进程，退出结果须有界确认。任何测试数据删除须列出精确对象和身份依据，不使用递归清理命令或隐式析构抹除失败现场。

实现采用独立集成测试目标 `lock_isolation`，复用 `thinws-p0-materialize` 的既有依赖和物化入口；不为仅验证系统 API 增加产品层或空库。冻结命令如下，目标接线之前不报告为已执行：

```bash
cargo test --locked -p thinws-p0-materialize --test lock_isolation -- --nocapture
cargo test --locked --release -p thinws-p0-materialize --test lock_isolation -- --nocapture
```

先以 L1/L2 的可失败断言建立最小双进程装置并确认 RED/GREEN，再补 L3–L7；每个子步的实际命令、命中的场景与失败记录写在本文，不新建实验时序规则文件。

首步只新增测试与测试接线、不改变已有生产库或解析规则，因此不对测试 oracle 自身运行变异，也不新增 fuzz target；现有生产模块证据的有效性按任务流程核对。若实施开始修改生产、安全判断或不可信输入解析，须先报告主 Agent 重新确定相应变异/fuzz 范围，不能借测试目录规避应有门禁。其他普通/真实平台门禁仍需执行。

2026-09-13 19:15 UTC 准入记录：主 Agent 已派发 GPT-5.6 Sol / `xhigh` 执行 L1/L2，写入区限定为独立测试与 manifest 接线；L3–L7 不在该首步派发范围，仍属本任务后续必需工作。主 Agent 维护文档及最终集成，规定模型的精确审核在相应候选冻结后进行；没有把编码 Agent 自查当作审核。

## 分步执行证据（2026-09-13）

下列记录按执行顺序保留各子步当时的候选、结果和未完成边界；当前剩余范围见文末，不用后续成功改写早期失败或未执行项。

准入文档提交 `9048ea1e6ae00240403d1ac81632ff2c54b4c546` 已由专用本机 PR Agent 使用 GPT-6 Astra / `xhigh` 精确审核，结论 Approve；该审核仅覆盖四份文档，不代表后续实验代码通过审核。

### L1/L2：别名冲突与独立副本

编码实际使用 GPT-5.6 Sol / `xhigh`，只新增 `experiments/p0/locks/lock_isolation.rs` 和 materialize 的 test target 接线。首次有效 RED 在持锁动作尚未接通时执行：独立竞争者确实获取同路径 `flock`，阳性冲突断言因此 exit 101；不是编译或环境失败。失败现场 `target/p0-lock-isolation-tests/fixture-parent-flock-52652-0/case` 保留，源文件 device/inode 为 `16777240/35550630`。

GREEN 后，主 Agent 核对并要求局部收紧协议绝对截止时间、spawn 前完成通信 FD 克隆、无子进程专用参数时的内部入口安全返回，以及按已登记身份/no-follow FD 执行写入；没有引入生产执行器或新依赖。编码侧最终 debug/release 各 1 个父测试通过、2 个内部入口默认 ignored；`--include-ignored` 为 3 passed。后者包含两个安全返回的内部入口，不能解释为三个独立矩阵场景；父测试内部实际覆盖两种锁、三种别名和两种物化方式。

冻结源码 SHA-256：`lock_isolation.rs` 为 `167642f9a3afb5b0854edf3a8f86c6d48fa7b863e3fad89afe443c657213ab5a`，materialize `Cargo.toml` 为 `474b39672cbbba7f12b86fbe6634f0e960f1dc0689cfcdda71f02ca6abe9e7a9`。主 Agent 在同一冻结文件上独立执行：

```bash
cargo fmt --all -- --check
cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
cargo test --locked --workspace --all-targets --quiet
cargo test --locked --release -p thinws-p0-materialize --test lock_isolation -- --include-ignored --nocapture
```

四条均 exit 0。普通 workspace 为 247 passed、0 failed、6 ignored：4 项原有额外 APFS 卷用例本次未执行，另 2 项为上述内部入口；嵌套 FD-zero 子进程不重复计数。release 锁目标为 3 passed、0 ignored，其场景计数按上一段解释。

本机 release 观察使用 APFS UUID `1A42C888-32E3-489C-9BFA-67FD640A94E8`、fsid `[16777240, 26]`。下表所有 inode 的 device 均为 `16777240`，三列顺序为源、APFS clone、Full Copy：

| 锁 API | 源 inode | clone inode | Full Copy inode | 实际结果 |
|---|---|---|---|---|
| `flock` | 35556526 | 35556532 | 35556535 | 三种源别名均冲突；两个副本可取锁；物化后及三方写入后源锁仍冲突 |
| POSIX `fcntl`，重叠全文件范围 | 35556547 | 35556553 | 35556556 | 同上；独立运行该 API，没有用跨 API 混测推导结论 |

真实 Receipt 中 clone 为 `Confirmed`、成功调用 2 次；Full Copy 为 `NotUsed`、clone 调用 0 次。两个普通源目录项包含一个硬链接别名，目标按既有物化契约成为独立普通文件。源/目标 manifest 及固定内容逐次断言通过；分别改写源、clone、Full Copy 不改变另两者的内容。这是本机 L1/L2 结果，不是所有缓存、文件系统或应用锁的保证。

主 Agent 本轮 release 现场为 `target/p0-lock-isolation-tests/fixture-parent-flock-8485-0/case` 和 `fixture-parent-posix-fcntl-8485-1/case`；测试正常握手释放并确认自有子进程退出，文件及早期失败现场均保留。未删除旧任务数据。

本步未执行新的变异或 fuzz：没有改变被测生产模块、既有安全规则、共享 fixture 或依赖，只增加系统 API 实验目标；不对测试 oracle 自身做变异。P0.b 收口仍需按任务流程核对已有生产变异证据的候选、范围及适用性，本步不作整体复用或收口通过声明。

首步代码及以上证据提交为 `f32a1fc6f31d56d42a465205d20aadb783b17a6e`。专用本机 PR Agent 使用 GPT-6 Astra / `xhigh` 对该精确提交正式 Approve，无阻断发现；其只读核对了三个文件、冻结哈希、真实锁断言与保留现场身份，没有重复执行耗时门禁。主 Agent 已将 `9048ea1` 和 `f32a1fc` 推送到远端任务分支，并用 `ls-remote` 核验分支指向完整 `f32a1fc` OID。当前尚未创建 P0-05 PR，也未触发本分支的 PR CI；首步审核和推送不代表整任务合并或完成。

首步收束后，继续由 GPT-5.6 Sol / `xhigh` 在 `experiments/p0/locks/` 依次补 L3/L4/L6 与 L5/L7；不修改共享 helper、生产模块或新建产品锁接口。L5 的本机工具准备核对了 `/usr/sbin/lsof 4.91` 的帮助和本机手册：字段输出和退出码可作诊断，但无匹配也可能 exit 1，不能把它冒充访问拒绝。该检查点只完成工具核对，没有执行扫描场景。

### L3/L4/L6：原地写、替换与时间属性

编码仍由 GPT-5.6 Sol / `xhigh` 完成，仅扩展独立锁测试。有效 RED 已完成真实物化和持锁握手，但尚未接入原地写，读到旧的 21 字节内容而非 L3 预期的 31 字节，因此 exit 101；`fixture-parent-flock-67998-0/case` 失败现场保留。编码侧冻结源码 SHA-256 为 `f4eec51ecb290efb58510ca428579ad1a8a212187d7486301164c55d5d8c833a`，随后继续在同一未提交子步补 L5/L7；此哈希不是已提交或正式审核候选。

编码侧对该冻结版本执行前文目标命令：debug/release 各 2 passed、2 个内部入口 ignored；加 `--include-ignored` 为 4 passed；针对性 Clippy、fmt 与 diff 检查均 exit 0。两项父测试分别组织 L1/L2 与 L3/L4/L6，每项内部都真实运行 `flock` 和 POSIX `fcntl`。主 Agent 已阅读完整差异，并通过只读 `stat` 独立核对下表保留文件的身份和最终长度；尚未把这次核对当作最终候选 Verification。

| 场景 | 本机观察 |
|---|---|
| L3 | 原地改写后源、clone、Full Copy 各自 inode 不变，内容分别为 31/29/25 字节且 BLAKE3 摘要互异；源文件在写前、写后及恢复时间后仍与独立竞争者冲突，实际 errno 均为 35 |
| L4 | 原子 rename 前后，父进程持有的旧 FD 与保留硬链接仍指向旧对象/旧摘要，竞争仍 errno 35；新路径是新 inode/新摘要并可取锁。替换后的登记表保留为历史，后续只通过已持有 FD 观察，不送回旧清理账本或隐式清理 |
| L6 | 顺序固定为读取长度、`pread` 内容摘要、再 `fstat`；记录 atime/mtime/ctime/birthtime 的秒和纳秒及 mode/nlink/length。`futimens` 后先不读内容、立即 `fstat`，两个 API 下三对象的 atime/mtime 均精确恢复到各自基准值；之后再读摘要验证内容不变，读操作可能影响 atime，不能把不同观察时点混为一组 |

最终 release 现场为 `target/p0-lock-isolation-tests/fixture-parent-flock-27756-1/case` 与 `fixture-parent-posix-fcntl-27756-2/case`，所有 device 均为 `16777240`。源/clone/Full Copy inode 分别是 `35557974/35557983/35557986` 和 `35558012/35558018/35558021`；L4 旧/新对象 inode 分别为 `35557993/35557996` 和 `35558041/35558044`。编码侧确认自有子进程按握手释放并退出，现场保留。

本步未尝试恢复 ctime，也没有断言读取内容后 atime 必须不变；记录的是本次 API/文件系统的时间属性观察，不推出全元数据保真、任何缓存命中或解除旧锁。L5/L7 完成后统一执行最终候选通用门禁、真实 release 验证与规定模型审核，避免为中间记录重复启动无关门禁。

### L5/L7：扫描边界、标记文件与交错复制

编码实际使用 GPT-5.6 Sol / `xhigh`，仍只扩展独立锁测试。有效 RED 已建立源 `application.lock`，尚未接通物化，读取副本标记因 `ENOENT` 而 exit 101；失败现场 `fixture-parent-flock-61824-0/case`（根 device/inode `16777240/35558380`）保留。GREEN 后覆盖全部三项父测试及各自两种锁 API，没有新增产品锁处理能力。

主 Agent 对最终冻结版本的本机 release 实测结果如下，完整字段与时间顺序见下节保留输出：

- L5：固定 `/usr/sbin/lsof` 实际 revision 为 `4.91`，只记录版本，不把它硬编码为支持条件。两个 API 的独立竞争者均 errno 35，而扫描虽可见 holder PID、FD、device/inode 和路径，锁字段均为空白；该字段不能证明无锁。释放后的活进程筛选无匹配为 exit 1、空输出，重新加锁后再次出现实际冲突和可见对象，不能把一次扫描当成持续保证。
- L5 拒绝访问探针：只将新建自有目录改为 mode 000，两次均实际得到 exit 1 和 `Permission denied`，与无匹配分开记录；通过已持有目录 FD 恢复后，主 Agent 用 `stat` 核验两个目录均为 0700，device/inode 分别为 `16777240/35565650` 和 `16777240/35565682`。扫描过程及输出有界，没有扫描真实用户缓存。
- L7：APFS clone 和 Full Copy 的标记副本身份独立、内核锁可取，但 `O_CREAT | O_EXCL` 均返回 `EEXIST(17)`。实际重写副本标记后源标记不变，副本路径仍存在、排他创建仍失败；源标记持内核锁时重写也仍与竞争者冲突。释放内核锁不会删除任何标记，本实验没有删除标记。
- L7：在自有文件上按固定次序执行真实读写，源状态依次为 `(0,0) → (1,0) → (1,1)`，两次文件复制均成功，副本却为源从未同时出现过的 `(0,1)`。这是逐文件复制的一致性反例，不是声称现有 `materialize_once` 发布了错误快照；不因此扩大产品一致性保证。

上述 release 的 L5/L7 现场为 `target/p0-lock-isolation-tests/fixture-parent-flock-28907-1/case` 与 `fixture-parent-posix-fcntl-28907-5/case`。源锁对象 inode 为 `35565565` 与 `35565677`，device 均为 `16777240`；holder 分别由 `28940 → 29049`、`29193 → 29275` 完成释放/重取。标记 clone Receipt 为 `Confirmed`、3 次真实调用，Full Copy 为 `NotUsed`、0 次 clone 调用。自有子进程已正常释放并确认退出，文件及失败现场保留。

### 最终冻结候选的本地门禁

最终源码 SHA-256 为 `ef91891ca62d59972dcbff5b53400525066b6ce87c98b8adcc55d43622f8a8c4`；manifest 仍为首步的 `474b39672cbbba7f12b86fbe6634f0e960f1dc0689cfcdda71f02ca6abe9e7a9`。主 Agent 在运行前后独立核对两者，完整执行前文四条命令，均取得 exit 0：

- fmt、Clippy 全 workspace/all targets/all features 通过；普通 workspace 为 **249 passed、0 failed、6 ignored**。4 项为本机未配置的额外 APFS 卷用例，2 项为内部子进程入口；嵌套 FD-zero 子进程不重复计数。
- release 锁目标加 `--include-ignored` 为 **5 passed、0 failed、0 ignored**，实测运行 2.69 秒。其中 3 项父测试组织 L1–L7、各自覆盖两种 API，另 2 项只是无专用调用参数时安全返回的内部入口，不是另两组锁场景。
- L1–L4/L6 在该最终版本也重新执行通过，不只沿用中间版本。没有改动旧生产库、原测试、共享 fixture、依赖、工具配置或排除项；没有执行新的变异测试。新增 `lsof` 字段读取限于固定工具/自有夹具的测试 oracle，不是生产外部输入接口，本轮未新增 fuzz target；此适用性一并交规定 Reviewer 核对。既有 fuzz/额外卷/供应链的当前候选 CI 仍待执行。

原始工具输出保存在本工作区 `target/p005-final-evidence.QTFMoy/common.log` 与 `release.log`，分别对应上述通用门禁和完整 release 结果。此前一次 release 调用因编排层保存未定义 `exit_code` 出错而丢失 session ID，只保留了 PID 12136 和部分输出，未观察到终态，不能记作测试通过或失败。主 Agent 确认该测试与 Cargo 已不在运行后，仅重跑短时 release 锁目标并完整保存 exit 0；首次部分输出及说明保留在 `release-partial-unconfirmed.log`，未删除或篡改原失败/未知记录。

### 既有变异证据的条件核对

GPT-6 Astra / `xhigh` 已按精确 `f32a1fc` 核对旧 Probe/materialize 源码、原测试、共享 fixture、工具与配置差异，认为截至该候选没有重新启动全量变异的依据。主 Agent 另直接读取 P0-01 与 P0-06 保留的 `outcomes.json` 汇总，原结果与各自实施记录一致；完整命令、计数和结果归属仍由 [P0-01 记录](P0-01_HostPathProbe实验.md)、[P0-06 记录](P0-06_原始目录镜像实验.md)维护。P0-07 的精确映射仍以 [P0-07 最终收口](P0-07_已跟踪检查与清理提示实验.md)为准，不重写其历史失败。

该审核发生在最终 L3–L7 冻结之前，不自动覆盖新候选。主 Agent 已核对最终差异仍限于独立测试与记录、通用门禁通过、权限恢复和自有子进程退出；最终 Reviewer 还须绑定精确提交核验上述条件及工具、SDK、平台和运行条件。变化部分按实际影响补测；本机未运行的额外卷用例不能记作通过，旧挂载点也不能当成仍可用的当前环境。该核对不外推旧 fuzz 环境、不代表 P0.b 放行，未运行新的变异测试。

## 验收与未执行项

调研 L1–L7 均为首轮范围，结果分别关联原矩阵编号，记录锁 API、实际 syscall/errno、时间顺序、设备与文件身份、内容摘要和实际可读时间属性。扫描只是辅助证据，锁隔离、元数据和多文件一致性分别判断；真实拒绝访问无法构造时如实记录，不用无匹配输出冒充拒绝访问验证。

通用门禁、双进程真实 APFS 测试、受影响逻辑的变异与适用 fuzz 按任务流程执行；适用性与范围须在实现前明确，不能事后为过门禁缩减。收口核对 P0.b 全部有效证据，不能仅凭本实验通过宣布小阶段完成。

当前首轮 L1–L7 及冻结候选本地门禁已完成，最终精确审核、审核后 Verification、PR CI 和合并尚待完成，任务未 Done。L8 因缺原始工具/版本、触发命令和脱敏错误而未执行，已向用户请求样例；此项不阻塞内核实验，不能据此宣称具体缓存问题已修复。Linux/Windows、网络文件系统、OverlayFS、卷镜像和具体缓存命中不在本轮实测范围。工作区和仅由本实验创建的数据为复查证据明确保留，不要求用户清理旧现场。

提交审核的 Go/No-Go 建议：**不继续实施“扫描＋重写”产品锁冲突处理**。本机 APFS 独立 clone/Full Copy 已隔离所测内核锁；原地写入不解除同一对象的锁，标记文件存在和多文件一致性又是不同问题。保留实验回归与既定原始目录边界，不新增解锁、缓存策略或执行包装。此建议沿用调研 §8 的既定决策条件，不改变公开 CLI/JSON/错误码，不代替任务最终审核，更不是 P0.b 或 P0/P1 阶段放行。
