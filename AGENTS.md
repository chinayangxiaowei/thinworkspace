# AGENTS.md

本文件是仓库内 AI Agent 的强制入口，适用于本目录及其全部子目录。

目标不是复制所有设计文档，而是：

1. 在这里固定任何任务都不能违反的硬约束；
2. 根据任务类型按需加载权威文档；
3. 在实现和交付前执行可检查的门禁。

文档不是可选背景材料。命中下述路由条件时，Agent 必须读取对应章节后再设计或修改代码。

---

## 零、仓库与文档目录

仓库根目录只保留项目入口、Agent 入口、许可证/发布元数据、构建/工具配置和一级工作区目录；普通项目文档与开发过程文档不得继续散放在根目录。当前目录索引由 [docs/README.md](docs/README.md) 管理。

| 路径 | 唯一职责 |
|---|---|
| `docs/project/architecture/` | 当前有效的产品架构、阶段边界和正式 ADR |
| `docs/project/design/` | 可指导多次实施的系统、模块、接口和跨平台详细设计 |
| `docs/project/reference/` | 技术选型、公开用户契约及长期参考资料 |
| `docs/development/process/` | 跨任务复用的研发流程和工程规范 |
| `docs/development/planning/` | 路线图、任务拆分、依赖、排期和阶段计划 |
| `docs/development/implementation/` | 某个任务或小阶段的实施记录、实验结果和收口证据 |
| `docs/development/review/` | 审核记录、阶段放行材料和待处理问题清单 |
| `LICENSE.md`、`LICENSING.md`、`COMMERCIAL-LICENSING.md`、`CHANGELOG.md` | 正式许可条款、授权解释、商业授权申请入口和面向版本的显著变更记录 |
| `experiments/p0/` | P0 可重复技术实验及其非产品代码 |
| `crates/`、`tests/`、`fuzz/`、`tools/` | Phase 1 源码、跨 crate 测试、模糊测试和仓库工具；具体边界以《技术栈》为准 |

归档和新增文档必须遵循以下规则：

1. 按“事实由谁维护、有效期多长”归档，不按标题中的“设计/实施”等字样机械判断。长期实施设计属于 `docs/project/design/`；只描述一次任务执行过程的实施记录属于 `docs/development/implementation/`。
2. 同一事实只保留一个权威落点，不得为了方便在 `project` 与 `development` 各复制一份。过程记录需要引用设计，审核结论需要引用被审对象。
3. 审核记录不是新的设计权威。审核结论改变产品、架构、接口或规则时，必须同步修改对应权威文档；不能只把最终语义留在 `review/`。
4. 只有出现独立职责、独立读者或独立生命周期时才新建文档。新文档必须声明“职责”和“非职责”，放入唯一匹配目录，并更新 `docs/README.md`；不得把无关内容堆入现有文件，也不得把同一内容拆散到多个文件。
5. 移动、重命名或废弃文档时，必须在同一变更中更新 `AGENTS.md`、`docs/README.md` 和仓库内相对链接。历史材料应进入明确的归档位置，不得与当前权威文档并列而不标状态。
6. 空目录和占位文件只表示规划中的物理布局，不表示对应能力已实现；不得据此绕过实施计划的依赖和验收条件。
7. 原样采用的标准许可证正文不得插入项目说明或自行改写；“职责/非职责”和中文授权边界写入 `LICENSING.md`，商业授权场景、申请流程和联系方式写入 `COMMERCIAL-LICENSING.md`。这些许可文件和 Changelog 属于根目录约定文件，不移动到 `docs/`。

---

## 一、权威文档

| 主题 | 权威文档 | 约束范围 |
|---|---|---|
| 项目品牌、仓库名和技术命名空间 | [仓库入口](README.md) | 决定 `ThinWorkspace`、`thinworkspace` 和 `thinws` 的用途 |
| 产品范围、阶段、架构方向和能力边界 | [产品与架构演进方案 v2.1](docs/project/architecture/ThinWorkspace_产品与架构演进方案_v2.1.md) | 决定做什么、何时做以及如何演进 |
| Phase 1 内部领域、数据布局、Git、状态机和恢复 | [Phase 1 单机 CLI 详细设计](docs/project/design/ThinWorkspace_Phase1单机CLI详细设计_v1.0.md) | 决定 Phase 1 内部组件如何协作 |
| 工作区物化、能力检测、跨卷和 Adapter 语义 | [跨平台工作区物化设计](docs/project/design/ThinWorkspace_跨平台工作区物化设计_v1.0.md) | 决定 Probe/Plan/Receipt 和平台实现契约 |
| Rust、crate、Adapter、依赖和测试工具 | [技术栈](docs/project/reference/技术栈.md) | 决定使用什么技术以及依赖边界 |
| 编码、安全、状态机、测试和评审规则 | [开发规范](docs/development/process/开发规范.md) | 决定代码必须如何实现和验证 |
| 任务准入、拆分、风险和完成条件 | [任务流程](docs/development/process/任务流程.md) | 决定研发工作如何流转 |
| P0/P1 任务编号、依赖和小阶段 | [Phase 1 实施计划](docs/development/planning/ThinWorkspace_Phase1实施计划_v1.0.md) | 决定当前按什么顺序交付 |
| CLI 命令、输出、错误码和用户可见行为 | [Phase 1 用户操作手册](docs/project/reference/ThinWorkspace_Phase1用户操作手册_v1.0.md) | 决定公开交互契约 |
| 项目许可、商业使用和对外授权术语 | [授权与商业使用声明](LICENSING.md)及[正式许可证](LICENSE.md) | 决定源码可用范围、商业授权边界和对外称谓 |
| 商业授权类型、申请材料、流程和联系方式 | [商业授权申请说明](COMMERCIAL-LICENSING.md) | 决定如何发起商业授权申请；不代替签署的商业协议 |

若外部归档中存在 `多Agent并行开发协作平台_产品与架构演进方案_v2.0.md`，它仅是历史材料；当前仓库不依赖该文件。除非任务明确比较旧版，不得以它覆盖 v2.1。

不同文档负责不同类型的事实，不使用笼统的全文优先级：

- 阶段、产品边界和演进方向以架构方案为准；
- Phase 1 内部领域、生命周期与 Git 设计以 Phase 1 详细设计为准；
- 物化、跨卷和 Adapter 语义以跨平台物化设计为准；
- 已发布或已冻结的 CLI 行为以用户操作手册为准；
- 技术选型以技术栈为准；
- 工程质量和安全规则以开发规范为准；
- 任务状态和交付门禁以任务流程为准。
- 当前任务顺序、依赖和小阶段以 Phase 1 实施计划为准。
- 项目授权模式和商业使用边界以 `LICENSE.md`、`LICENSING.md` 为准；商业授权申请入口以 `COMMERCIAL-LICENSING.md` 为准。

同一事实只允许有一个详细权威来源。其他文档可以描述与自身读者相关的结果或约束，但不得再复制常量、字段集、算法、系统调用步骤或完整测试矩阵。需要新的独立设计域时可以新建文档，但必须先声明职责和非职责，并从原文档移除重复内容。

如果两个权威文档对同一事实冲突，不得静默选择其中一个。先指出冲突并同步文档；冲突会实质改变实现或公开行为时，必须请求用户决策。

---

## 二、阶段词汇

设计和用户文档使用完整名称 `Phase 0` 至 `Phase 5`。

研发任务、分支、提交和工程规范使用简写：

- `P0` = `Phase 0（关键技术验证）`；
- `P1` = `Phase 1（单机 CLI MVP）`；
- 后续阶段按相同方式映射。

当前允许实施的范围是 P0/P1。不得因为代码可能“以后有用”而提前引入 P2–P5 的领域对象、服务或空抽象。

---

## 三、按需加载协议

### 3.1 所有代码变更

必须读取：

- 《开发规范》中与修改相关的完整章节；
- 《技术栈》中对应 crate、Adapter、测试和依赖章节。

### 3.2 按任务类型追加读取

| 任务涉及 | 必须追加读取 |
|---|---|
| 阶段、范围、架构或阶段能力 | 架构方案相关完整章节和阶段能力边界表 |
| CLI 命令、参数、输出、JSON、错误码 | 用户操作手册对应章节；开发规范的 CLI 与兼容章节 |
| Workspace 创建、删除、恢复、GC | Phase 1 详细设计的状态机、查询恢复和 GC 章节；开发规范相关章节 |
| init、bootstrap config 或 data root 身份 | Phase 1 详细设计的实例与 data root 章节；技术栈的实现选型章节 |
| APFS、CoW、跨卷、路径或符号链接 | 跨平台物化设计相关完整章节；技术栈的 macOS/APFS 选型章节 |
| Git、Base、branch、worktree | Phase 1 详细设计的 Repository/Git 章节；技术栈和开发规范相关章节 |
| exec、信号、超时、进程清理 | Phase 1 详细设计的 Execution 章节；用户手册的可见契约；技术栈和开发规范相关章节 |
| 新依赖、工具链或 CI | 技术栈的选型、测试、供应链和发布章节 |
| 许可证、商业使用、贡献权属、商业授权申请或对外授权称谓 | 完整读取 `LICENSE.md`、`LICENSING.md` 与 `COMMERCIAL-LICENSING.md`；涉及依赖时追加技术栈的供应链章节 |
| 版本发布或显著变更记录 | `CHANGELOG.md`；追加任务流程的阶段收口与放行章节 |
| 任务拆分、并行 Agent、风险、验收或放行流程 | 《任务流程》对应完整章节 |
| Agent 编码或审核派发、模型选择 | 《任务流程》§8.3 模型分工；在任务证据中记录实际模型 |
| 任务编号、依赖、当前小阶段或计划调整 | 《Phase 1 实施计划》对应完整章节 |
| 变异测试或模糊测试 | 技术栈测试章节；开发规范测试章节；任务流程专项门禁 |

只需按需读取，不要求每次把全部文档装入上下文。读取方法：

1. 先用标题或关键词定位章节；
2. 阅读命中章节的完整内容及其直接引用的约束；
3. 修改跨越多个主题时，取各路由要求的并集；
4. 架构评审、阶段调整、全局重构或发布验收必须通读全部当前文档；
5. 不得只依赖旧对话摘要、搜索命中的单行文字或本文件的概述替代详细契约。

---

## 四、跨任务硬约束

本节只保留遗漏后果严重、且几乎所有任务都适用的约束。具体数据结构、系统调用、命令参数、错误码和测试矩阵必须从第一节的单一权威来源读取，不在这里复制。

### 4.1 阶段与依赖

- 当前只实施 P0/P1；不得预建后续阶段的领域对象、Port、crate、daemon 或空占位实现。
- P1 是单机 CLI，不引入常驻服务、HTTP/gRPC、消息队列、远程控制面或异步运行时。
- 依赖方向固定为 `cli → application → core/ports`，Adapter 实现 Port；Core/Application 不直接调用具体 OS、Git CLI 或 SQLite API。
- Port 的完整名单和语义以 Phase 1 详细设计为准；未先更新该设计和相关 ADR，不得新增平行抽象。

### 4.2 数据安全与可恢复性

- 不得静默降级、把预检当成执行成功、把目录存在当成 Ready，或在无法证明安全时覆盖/删除/迁移数据。
- 文件物化必须遵循《跨平台工作区物化设计》的 Probe/Plan/Revalidate/Execute/Receipt 契约；Adapter 只报告事实，降级政策由 Core/Application 决定。
- 受控对象必须由类型 ID 和已验证根目录推导；禁止任意 Workspace 路径、路径逃逸、跟随未验证符号链接，或用 shell/glob/未验证环境变量选择删除目标。
- Git、文件系统和 SQLite 不存在跨系统事务；生命周期写入必须有过渡状态/operation、Receipt 和幂等协调路径。
- Git 与子进程使用 argv 调用，不通过 shell 拼接；`workspace exec` 始终是 trusted-host，不得宣称为 Sandbox。
- 不记录密钥、完整环境、完整 argv、源码正文或未脱敏凭据 URL。

### 4.3 公开契约与质量

- 用户可见命令、stdout/stderr、JSON、退出码和破坏性参数语义只以 Phase 1 用户手册为准；实现不得自行创造第二套契约。
- Core/Port 使用结构化错误；CLI 添加上下文时必须保留底层类型和稳定错误码。
- Core、Application 和 CLI 禁止 unsafe；必要 FFI 只能收敛在小型平台 Adapter 内，有 `SAFETY:` 不变量和专项验证。
- 真实 APFS/Git 行为不能只用 mock 代替；测试种类、执行时机和门槛分别以《开发规范》和《任务流程》为准。
- 环境或仓库尚不具备某项门禁时，必须报告“未执行”和原因，不得以人工阅读代替并宣称已保证。

---

## 五、任务执行规则

### 5.1 开始前

1. 判断任务属于 P0 还是 P1、归属哪个小阶段，确认该小阶段已有退出条件，并标记《任务流程》中的 R1–R4 风险等级。
2. 按第三节加载所需文档；先确认公开契约、状态影响、失败行为和范围外事项。
3. 检查工作区现状并保留用户已有修改；不得覆盖或回退无关变更。
4. 涉及架构、状态机、BaseId、降级策略、SQLite 主模型或安全边界时，先确认是否需要 ADR。
5. 超出 P1 边界时停止实施，转为需求或 ADR，不用隐藏开关偷渡功能。

### 5.2 实施中

- 一个任务只交付一个明确结果，并尽量保持一个主要写入区域。
- 小问题优先做局部、可验证的根因修复。禁止为了绕过一个小问题引入只有单一调用者的新抽象、平行状态/机制、额外依赖、隐式 fallback 或超出任务边界的通用框架；“以后可能有用”不能作为增加复杂度的理由。
- 如果当前修复开始连续制造次生问题，或每解决一层又必须新增一层规则、兼容分支或补丁，应立即停止问题套娃：不再沿现方案堆叠复杂度，回到原始问题、既有模型和最小验收断言重新判断。主 Agent 负责在任务实施和评审中主动识别并纠偏此类趋势。
- 子 Agent 严禁自行把问题复杂化。确实无法在现有边界内安全解决时，必须向主 Agent 报告原始问题、已验证证据、简单方案为何不足、拟增加的复杂度及影响范围，并暂停扩项；由主 Agent 决定简化后当前处理、拆成独立任务并排期，或修改对应权威文档和实施计划，明确承认该缺口、限制与暂不实现的边界。未经主 Agent 决定，不得继续增加机制掩盖缺口。
- 每个行为变更遵循《任务流程》的“对齐 → RED → GREEN → 重构 → 本地门禁 → 自查 → 提交”；RED 必须确认测试因预期原因失败。
- Adapter 返回结构化证据；禁止用布尔 `supported=true` 表达不同保证强度。
- 外部操作前记录 operation/过渡状态，完成后记录 Receipt；失败保留可解释状态。
- 用户可见行为、错误码或阶段边界变化时同步修改对应权威文档。
- 测试和评审证据写入已有任务、PR、CI 或阶段状态载体；不新建独立 Agent 审核轮次或交接报告。
- 子 Agent 发现规则或文档错误时不得擅自修改；必须把位置、证据、影响和修改建议通知主 Agent，由主 Agent 核验并决定是否及如何修改。错误会影响实现或安全边界时，暂停受影响工作。

### 5.3 变更联动

| 变化 | 必须修改的单一来源 | 只做联动检查的位置 |
|---|---|---|
| CLI 命令、输出或错误码 | 用户手册 | `--help`、fixture 和 CLI 测试；其他文档只检查引用是否失效 |
| Workspace 状态、恢复或 Git 领域语义 | Phase 1 详细设计 | migration、故障注入和 Git 集成测试 |
| PlatformProbe、Materializer 或跨卷语义 | 跨平台物化设计 | 平台 Adapter、真实文件系统测试和技术选型 |
| 阶段名称、范围或演进边界 | 架构方案 | 任务阶段映射和用户手册的可见边界 |
| 新依赖、系统 API 或质量工具 | 技术栈 | lockfile、供应链检查和 CI |
| 通用编码/安全规则 | 开发规范 | lint、架构测试和评审检查项 |
| 任务时序、风险或门禁 | 任务流程 | 任务模板和 CI 入口 |

“联动检查”不等于把新内容复制到每个文档。只有该文档自身读者可见的结果发生变化时才修改其文字；否则保留引用。

### 5.4 完成前

必须完整执行《任务流程》“通用门禁”中的当前命令，并按该文档的“任务专项门禁”和当前小阶段/阶段门禁选择附加验证；完整命令和测试清单不在本文件重复。日常增量测试不能替代收口门禁。

如果仓库尚未具备某项命令或环境，必须明确报告“未执行”及原因，不能写成已通过。

交付说明必须包含：完成内容、影响的公开契约、验证结果、未执行项和剩余风险。

Agent 不能自行宣布阶段放行或代替人工签字；阶段门禁通过后仍需人类确认，之后才能按发布策略打 tag。

---

## 六、禁止通过压缩文档规避约束

- 不得把本文件当作其他文档的替代品。
- 不得因为上下文有限而跳过命中路由的完整章节。
- 不得复制一份长期不同步的规则摘要到源码子目录。
- 子目录需要特殊规则时可以增加更近的 `AGENTS.md`，但只能细化职责、命令和局部约束，不能削弱本文件的数据安全、阶段或公开契约。
- 发现本文件与权威文档过期时，优先修正引用和摘要；不要在实现中自行创造第三种语义。

---

## 七、遵守机制与维护原则

`AGENTS.md` 是 Agent 的行为约束，不是安全边界。可机器验证的规则必须同时落实到构建和 CI：

- crate 依赖方向由 workspace 结构和依赖检查验证；
- 格式、静态检查和普通测试由 rustfmt、Clippy 和 Cargo test 验证；
- 依赖与许可证由 lockfile、`cargo-deny` 和审计任务验证；
- CLI、JSON 和错误码由 fixture/契约测试验证；
- 状态机、安全策略由单元测试、故障注入和 `cargo-mutants` 验证；
- 不可信输入由 `cargo-fuzz` 验证；
- APFS、Git 和删除语义由真实平台集成测试验证。

在这些自动化尚未建成前，Agent 必须把缺失门禁列为未完成项，不能仅凭代码评审宣称对应约束已被保证。

维护本文件时遵循：

1. 仅把跨任务、稳定且遗漏后果严重的规则写入“硬约束”；
2. 具体数据结构、完整命令示例和长测试矩阵保留在权威文档；
3. 新增细节优先更新路由表，不复制整段原文；
4. 权威文档改变硬约束时，在同一个变更中同步本文件；
5. 定期检查链接、阶段映射和摘要是否仍与权威文档一致。
6. 目录分类或文档位置变化时，同步维护第零节与 `docs/README.md`，但不在两处复制文档正文。
