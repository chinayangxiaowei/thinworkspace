[package]
name = "thinws-p0-materialize"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[lib]
name = "thinws_p0_materialize"

[dependencies]
blake3 = "1"
libc = "0.2"
thinws-p0-probe = { path = "../probe" }
thiserror = "2"

[dev-dependencies]
rustix = { version = "1", features = ["fs"] }
